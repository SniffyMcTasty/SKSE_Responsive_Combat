; Install beside ResponsiveCombat.dll in Data/SKSE/Plugins.
; Read once at startup. Missing or invalid individual values use these defaults.
[General]
SchemaVersion=1
Enabled=true

[Logging]
; trace, debug, info, warn, error, critical, off
; Startup diagnostics are always recorded before this filter takes effect.
LogLevel=info

[Diagnostics]
; Passive player input and animation recording. Requires Enabled=true and info or more verbose logging.
TraceCombat=false
